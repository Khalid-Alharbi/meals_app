import 'package:flutter/material.dart';

class FavoriesScreen extends StatelessWidget {
  const FavoriesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("The Favorties!"),
    );
  }
}
